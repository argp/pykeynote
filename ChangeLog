- Added egg and easy_install support.

- Successful testing with Python 2.5.

- keynote.decode_key() now handles private keys correctly.

- keynote.encode_key() now takes keytype from the deckey object.

pykeynote-1.0:

- Public release.

pykeynote-0.9:

- Ported various NTRG/CTVR internal applications from C KeyNote to
  Python and pykeynote.

- Minimal, but successful, testing on OpenBSD.

pykeynote-0.8:

- Initial internal release.

- Added documentation.

- Unit testing.

- ENVIRONMENT_FLAG_FUNC (callback) support.

- Pure Python (Pyrex) implementation of kn_read_asserts().

pykeynote-0.7:

- sample_app.py.

- Implemented the keynote_error class for error handling.

- Moved from SWIG to Pyrex for the low-level glue code.

$Id$
